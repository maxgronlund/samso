= simple_form_for [ @admin_system_message ] do |f|
  = f.error_notification

  .form-inputs
    = f.input :title
    = f.input :body, input_html: {class: 'system-message-form'}

  .form-actions
    = f.submit t('save'), class: 'btn btn-primary'

javascript:
  $(function() {
    $('#admin_system_message_body').froalaEditor(
      {
        toolbarButtons: [ 'bold', 'italic', 'underline', '|', 'paragraphFormat', 'align', 'formatOL', 'formatUL', 'outdent', 'indent', 'insertLink', '|', 'insertHR', 'selectAll', 'clearFormatting', 'undo', 'redo']
      }
    )
  });