en:
  activerecord:
    attributes:
      page_col_module:
        moduleable_type: Type
        access_to: Show to
    models:
      page_col_module: Module type

  page_col_module:
    add_module: Add module
    show:
    show:
      all: Alle guests
      guests_only: Only when not logged in
      members_only: When logged in
      subscribers_only: When logged in with a valid subscriptions
      expired_subscribers: When logged and subscription is expired
